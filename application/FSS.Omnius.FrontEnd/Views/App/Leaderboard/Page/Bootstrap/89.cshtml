@{ Layout = "~/Views/Shared/_OmniusUserAppLayout.cshtml"; }@{ Dictionary<string, string> formState = (Dictionary<string, string>)ViewData["formState"]; }<div class="mozaicBootstrapPage"><div class="container"><div class="row"><div class="col-xs-12"><h1 class="">@(@"Leaderboard")</h1></div></div><div class="row"><div class="col-xs-12"><div class="panel panel-default" ><div class="panel-body">
@{ if(ViewData.ContainsKey("tableData_leaderboard_dataTable") && ((System.Data.DataTable)(ViewData["tableData_leaderboard_dataTable"])).Rows.Count > 0) {
    <table class="table data-table no-footer dataTable" data-dtpaging="0" data-dtinfo="0" data-dtfilter="0" data-dtordering="0" data-dtcolumnfilter="0" data-dtserverside="0" id="leaderboard_dataTable" role="grid" aria-describedby="leaderboard_dataTable_info" style="">
        <thead>
            <tr>
            @foreach (System.Data.DataColumn col in ((System.Data.DataTable)(ViewData["tableData_leaderboard_dataTable"])).Columns) {
                <th>@col.Caption</th>
            }
                
            </tr>
        </thead>
        <tfoot>
            <tr>@foreach (System.Data.DataColumn col in ((System.Data.DataTable)(ViewData["tableData_leaderboard_dataTable"])).Columns) {
                <th>@col.Caption</th>
            }
                
            </tr>
        </tfoot>
        <tbody>
        @foreach(System.Data.DataRow row in ((System.Data.DataTable)(ViewData["tableData_leaderboard_dataTable"])).Rows) {
            <tr>@foreach (var cell in row.ItemArray) {
                <td>@cell.ToString()</td>
            }
                
            </tr>
        }
        </tbody>
    </table>
} else {
    <div class="alert alert-info empty-table-label">Tabulka neobsahuje žádná data</div>
} }
<input type="hidden" name="leaderboard_dataTable" />
</div></div></div></div><div class="row"><div class="col-xs-12"><form class="form-horizontal" method="post"><div class="panel panel-default" ><div class="panel-heading"><span class="">@(@"Find your position in leaderboard")</span></div><div class="panel-body"><div class="form-group col-sm-10"><label for="" class="col-sm-2">@(@"Email:")</label><div class="col-sm-8"><input type="text" name="email_input" value="@(formState.ContainsKey("email_input") ? formState["email_input"] : (ViewData.ContainsKey("inputData_email_input") ? @ViewData["inputData_email_input"] : ""))" class="form-control mbe-active" id="email_input" required="required" /></div></div><div class="col-sm-1"><button type="submit" class="btn btn-primary" name="button" value="find_button" id="find_button" >@(@"Find")</button></div></div></div>@Html.AntiForgeryToken()</form></div></div><div class="row"><div class="col-xs-12">
@if((ViewData.ContainsKey("yourPosition_if") && (bool)ViewData["yourPosition_if"]) == true) {
<div class="" data-varname="yourPosition_if" id="yourPosition_if"><div class="panel panel-default" ><div class="panel-heading"><span class="">@(@"Your position")</span></div><div class="panel-body">
@{ if(ViewData.ContainsKey("tableData_position_dataTable") && ((System.Data.DataTable)(ViewData["tableData_position_dataTable"])).Rows.Count > 0) {
    <table class="table data-table no-footer dataTable" data-dtpaging="0" data-dtinfo="0" data-dtfilter="0" data-dtordering="0" data-dtcolumnfilter="0" data-dtserverside="0" id="position_dataTable" role="grid" aria-describedby="leaderboard_dataTable_info" style="">
        <thead>
            <tr>
            @foreach (System.Data.DataColumn col in ((System.Data.DataTable)(ViewData["tableData_position_dataTable"])).Columns) {
                <th>@col.Caption</th>
            }
                
            </tr>
        </thead>
        <tfoot>
            <tr>@foreach (System.Data.DataColumn col in ((System.Data.DataTable)(ViewData["tableData_position_dataTable"])).Columns) {
                <th>@col.Caption</th>
            }
                
            </tr>
        </tfoot>
        <tbody>
        @foreach(System.Data.DataRow row in ((System.Data.DataTable)(ViewData["tableData_position_dataTable"])).Rows) {
            <tr>@foreach (var cell in row.ItemArray) {
                <td>@cell.ToString()</td>
            }
                
            </tr>
        }
        </tbody>
    </table>
} else {
    <div class="alert alert-info empty-table-label">Tabulka neobsahuje žádná data</div>
} }
<input type="hidden" name="position_dataTable" />
</div></div></div>
}</div></div></div></div><script type="text/javascript" src="/Scripts/UserScripts/Application/Leaderboard/leaderboardJS.js"></script>