@using FSS.Omnius.FrontEnd.Utils@{T t = new T( (FSS.Omnius.Modules.CORE.Locale)ViewData["locale"] );}@{ Layout = "~/Views/Shared/_OmniusUserAppLayout.cshtml"; }@{Dictionary<string, string> formState = (Dictionary<string, string>)ViewData["formState"]; }<form class="mozaicForm" method="post" enctype = "multipart/form-data"><div id="uic_label0" name="label0"  class="uic form-heading" contentTemplate="" style="left: 20px; top: 20px; width: 146px; height: 42px; ">@Html.Raw(ViewData.ContainsKey("inputData_label0") ? "Form heading".Replace("{var1}", (ViewData["inputData_label0"] ?? "").ToString()) : t._("Form heading") )</div>@{ if(ViewData.ContainsKey("tableData_data-table-with-actions1") && ((System.Data.DataTable)(ViewData["tableData_data-table-with-actions1"])).Rows.Count > 0) {<table id="uic_data-table-with-actions1" name="data-table-with-actions1" true  class="uic data-table data-table-with-actions display hideFirst" style="left: 20px; top: 80px; width: 1000px; height: 151px; " uicWidth="1000px"><thead><tr>@foreach (System.Data.DataColumn col in ((System.Data.DataTable)(ViewData["tableData_data-table-with-actions1"])).Columns) {<th>@(t._(col.Caption))</th>} <th>@(t._("Akce"))</th></tr></thead><tfoot><tr>@foreach (System.Data.DataColumn col in ((System.Data.DataTable)(ViewData["tableData_data-table-with-actions1"])).Columns) {<th>@(t._(col.Caption))</th>} <th>@(t._("Akce"))</th></tr></tfoot><tbody>@foreach(System.Data.DataRow row in ((System.Data.DataTable)(ViewData["tableData_data-table-with-actions1"])).Rows) {<tr>@foreach (var cell in row.ItemArray) {<td>@t._(cell.ToString())</td>}<td class="actionIcons"><i class="fa fa-edit rowEditAction"></i><i class="fa fa-remove rowDeleteAction"></i></td></tr>}</tbody></table>} else { <div class="uic control-label empty-table-label" style="left: 20px; top: 80px;">@t._("Tabulka neobsahuje žádná data")</div> } }<input type="hidden" name="data-table-with-actions1" /><input type="hidden" name="registry" value="@ViewData["crossBlockRegistry"]" />@Html.AntiForgeryToken()</form>
