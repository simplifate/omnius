@{ Layout = "~/Views/Shared/_OmniusUserAppLayout.cshtml"; }@{ Dictionary<string, string> formState = (Dictionary<string, string>)ViewData["formState"]; }<div class="mozaicBootstrapPage"><div class="container-fluid"><div class="row"><form class="form-horizontal" method="post"><div class="col-xs-12"><h1 class="">@(@"Overview pair config")</h1><button type="submit" class="btn btn-primary btn-lg" name="button" value="newPairConfig_button" id="newPairConfig_button" style="margin: 15px 0 30px 0;" >@(@"New pair config")</button>
@{ if(ViewData.ContainsKey("tableData_ParirConfig") && ((System.Data.DataTable)(ViewData["tableData_ParirConfig"])).Rows.Count > 0) {
    <table class="table data-table no-footer dataTable mbe-active" data-dtpaging="1" data-dtinfo="1" data-dtfilter="1" data-dtordering="1" data-dtcolumnfilter="1" id="ParirConfig" role="grid" aria-describedby="pairConfig_dataTable_info" data-dtserverside="1" style="">
        <thead>
            <tr>
            @foreach (System.Data.DataColumn col in ((System.Data.DataTable)(ViewData["tableData_ParirConfig"])).Columns) {
                <th>@col.Caption</th>
            }
                <th>Akce</th>
            </tr>
        </thead>
        <tfoot>
            <tr>@foreach (System.Data.DataColumn col in ((System.Data.DataTable)(ViewData["tableData_ParirConfig"])).Columns) {
                <th>@col.Caption</th>
            }
                <th>Akce</th>
            </tr>
        </tfoot>
        <tbody>
        @foreach(System.Data.DataRow row in ((System.Data.DataTable)(ViewData["tableData_ParirConfig"])).Rows) {
            <tr>@foreach (var cell in row.ItemArray) {
                <td>@cell.ToString()</td>
            }
                <td class="actionIcons"><i title="edit" class="fa fa-pencil" data-action="edit" data-idparam="modelId" data-confirm=""></i> <i title="delete" class="fa fa-remove" data-action="delete" data-idparam="deleteId" data-confirm="Opravdu chcete záznam smazat?"></i></td>
            </tr>
        }
        </tbody>
    </table>
} else {
    <div class="alert alert-info empty-table-label">Tabulka neobsahuje žádná data</div>
} }
<input type="hidden" name="ParirConfig" />
</div>@Html.AntiForgeryToken()</form></div></div></div>