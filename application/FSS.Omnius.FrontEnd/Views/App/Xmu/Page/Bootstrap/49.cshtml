@{ Layout = "~/Views/Shared/_OmniusUserAppLayout.cshtml"; }@{ Dictionary<string, string> formState = (Dictionary<string, string>)ViewData["formState"]; }<div class="mozaicBootstrapPage"><form class="form-horizontal" method="post"><div class="container"><div class="row"><div class="col-xs-12"><button class="btn btn-lg btn-primary" type="submit" name="button" id="newCategory_button" value="newCategory_button" style="margin-bottom: 30px; margin-top: 15px;" >@(@"New category")</button></div></div><div class="panel panel-default" ><div class="panel-heading"><h3 class="panel-title">@(@"Categories")</h3></div><div class="panel-body">
@{ if(ViewData.ContainsKey("tableData_category_dataTable") && ((System.Data.DataTable)(ViewData["tableData_category_dataTable"])).Rows.Count > 0) {
    <table class="table data-table no-footer mbe-active dataTable" data-dtpaging="1" data-dtinfo="0" data-dtfilter="0" data-dtordering="0" data-dtcolumnfilter="0" id="category_dataTable" role="grid" aria-describedby="category_dataTable_info" data-dtserverside="0" style="">
        <thead>
            <tr>
            @foreach (System.Data.DataColumn col in ((System.Data.DataTable)(ViewData["tableData_category_dataTable"])).Columns) {
                <th>@col.Caption</th>
            }
                <th>Akce</th>
            </tr>
        </thead>
        <tfoot>
            <tr>@foreach (System.Data.DataColumn col in ((System.Data.DataTable)(ViewData["tableData_category_dataTable"])).Columns) {
                <th>@col.Caption</th>
            }
                <th>Akce</th>
            </tr>
        </tfoot>
        <tbody>
        @foreach(System.Data.DataRow row in ((System.Data.DataTable)(ViewData["tableData_category_dataTable"])).Rows) {
            <tr>@foreach (var cell in row.ItemArray) {
                <td>@cell.ToString()</td>
            }
                <td class="actionIcons"><i title="Edit" class="fa fa-edit" data-action="edit" data-idparam="modelId" data-confirm=""></i> <i title="delete" class="fa fa-remove" data-action="delete" data-idparam="deleteId" data-confirm="Opravdu chcete záznam smazat?"></i></td>
            </tr>
        }
        </tbody>
    </table>
} else {
    <div class="alert alert-info empty-table-label">Tabulka neobsahuje žádná data</div>
} }
<input type="hidden" name="category_dataTable" />
</div></div></div>@Html.AntiForgeryToken()</form></div>