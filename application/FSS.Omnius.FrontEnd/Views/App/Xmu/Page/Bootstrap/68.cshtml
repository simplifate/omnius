@{ Layout = "~/Views/Shared/_OmniusUserAppLayout.cshtml"; }@{ Dictionary<string, string> formState = (Dictionary<string, string>)ViewData["formState"]; }<div class="mozaicBootstrapPage"><form class="form-horizontal" method="post"><div class="container"><div class="row"><div class="col-xs-12"><button class="btn btn-lg btn-primary" type="submit" name="button" id="newBalance_button" value="newBalance_button" style="margin-bottom: 30px; margin-top: 15px;" >@(@"New balance")</button></div></div><div class="panel panel-default mbe-active" ><div class="panel-heading"><h3 class="panel-title">@(@"Hot & cold wallets balances")</h3></div><div class="panel-body">
@{ if(ViewData.ContainsKey("tableData_balance_dataTable") && ((System.Data.DataTable)(ViewData["tableData_balance_dataTable"])).Rows.Count > 0) {
    <table class="table data-table no-footer dataTable" data-dtpaging="1" data-dtinfo="0" data-dtfilter="0" data-dtordering="0" data-dtcolumnfilter="0" id="balance_dataTable" role="grid" aria-describedby="DataTables_Table_0_info">
        <thead>
            <tr>
            @foreach (System.Data.DataColumn col in ((System.Data.DataTable)(ViewData["tableData_balance_dataTable"])).Columns) {
                <th>@col.Caption</th>
            }
                <th>Akce</th>
            </tr>
        </thead>
        <tfoot>
            <tr>@foreach (System.Data.DataColumn col in ((System.Data.DataTable)(ViewData["tableData_balance_dataTable"])).Columns) {
                <th>@col.Caption</th>
            }
                <th>Akce</th>
            </tr>
        </tfoot>
        <tbody>
        @foreach(System.Data.DataRow row in ((System.Data.DataTable)(ViewData["tableData_balance_dataTable"])).Rows) {
            <tr>@foreach (var cell in row.ItemArray) {
                <td>@cell.ToString()</td>
            }
                <td class="actionIcons"><i title="Edit" class="fa fa-edit" data-action="edit" data-idparam="modelId" data-confirm=""></i> <i title="delete" class="fa fa-remove" data-action="delete" data-idparam="deleteId" data-confirm=""></i></td>
            </tr>
        }
        </tbody>
    </table>
} else {
    <div class="alert alert-info empty-table-label">Tabulka neobsahuje žádná data</div>
} }
<input type="hidden" name="balance_dataTable" />
</div></div></div>@Html.AntiForgeryToken()</form></div>