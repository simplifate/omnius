@{ Layout = "~/Views/Shared/_OmniusUserAppLayout.cshtml"; }@{ Dictionary<string, string> formState = (Dictionary<string, string>)ViewData["formState"]; }<div class="mozaicBootstrapPage"><div class="container"><div class="panel panel-default" ><div class="panel-heading"><h3 class="panel-title">@(@"Cryptowat pairs (JToken)")</h3></div><div class="panel-body"><ul class="">
@{ if(ViewData.ContainsKey("cryptowat")) {
        foreach(var row in (Newtonsoft.Json.Linq.JToken)ViewData["cryptowat"]) {
            <li class="">
                <span class="">@(Convert.ToString(FSS.Omnius.Modules.Tapestry.KeyValueString.ParseValue("row.base.name", new Dictionary<string, object>() { { "row", row } } )))</span><span class="">@(@"   =>    ")</span><span class="">@(Convert.ToString(FSS.Omnius.Modules.Tapestry.KeyValueString.ParseValue("row.quote.name", new Dictionary<string, object>() { { "row", row } } )))</span>   
            </li>
        }
    }
}</ul></div></div><div class="panel panel-default" ><div class="panel-heading"><h3 class="panel-title">@(@"Employees (List<Dictionary<string, string>>)")</h3></div><div class="panel-body"><ul class="">
@{ if(ViewData.ContainsKey("emp")) {
        foreach(var row in (List<object>)ViewData["emp"]) {
            <li class="">
                <span class="">@(Convert.ToString(FSS.Omnius.Modules.Tapestry.KeyValueString.ParseValue("row[name]", new Dictionary<string, object>() { { "row", row } } )))</span><span class="">@(@"   =>    ")</span><span class="">@(Convert.ToString(FSS.Omnius.Modules.Tapestry.KeyValueString.ParseValue("row[surname]", new Dictionary<string, object>() { { "row", row } } )))</span>   
            </li>
        }
    }
}</ul></div></div></div></div>